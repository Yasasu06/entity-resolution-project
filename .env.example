# Copy this file to .env and fill in the real values.
#
#     cp .env.example .env
#
# .env is gitignored and must never be committed. .env.example is committed
# and must never contain a real credential.

OPENAI_API_KEY=paste-your-key-here
